---
title: lvm2
---

# lvm2

A minimal Docker image providing the lvm2 suite of tools (`pvcreate`, `vgcreate`, `lvcreate`, `lvextend`, etc.), built from source at a pinned version. Used as a dependency image for [linstor-provision-disk](./linstor-provision-disk.md); not intended to be run standalone.
