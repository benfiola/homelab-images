apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: {{ include "pvc-restore.name" . }}
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: {{ include "pvc-restore.name" . }}
subjects:
  - kind: ServiceAccount
    name: {{ include "pvc-restore.name" . }}
    namespace: {{ .Release.Namespace }}
