apiVersion: v2
name: vault-push-secrets
description: A helm chart that deploys a workload capable of dumping Vault secrets and pushing them to Bitwarden Secrets Manager
type: application
version: 0.0.0
appVersion: "0.0.0"
