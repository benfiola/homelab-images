apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: {{ include "gateway-route-sync.name" . }}
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: {{ include "gateway-route-sync.name" . }}
subjects:
  - kind: ServiceAccount
    name: {{ include "gateway-route-sync.name" . }}
    namespace: {{ .Release.Namespace }}
